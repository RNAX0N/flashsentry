# PR triage (2026-05-28)

**Goal:** Get a working Arch install on `main`, then land UX/features in a sane order.

All listed PRs below had **green CI** at triage time except where noted.

---

## Merge order (recommended)

| Order | PR | Action | Why |
|------:|-----|--------|-----|
| **1** | [#26](https://github.com/RNAX0N/flashspartan/pull/26) Fix Arch install | **Merge** (squash OK) | Unblocks `packaging/build-package.sh -si`. Without this, `makepkg` hits a non-existent `archive/v1.4.2` tarball. |
| **2** | [#23](https://github.com/RNAX0N/flashspartan/pull/23) UI readme parity | **Merge** after #26 | Self-contained UI polish; rebase on `main` if needed. |
| **3** | [#24](https://github.com/RNAX0N/flashspartan/pull/24) Verify history + wizard + Paranoid | **Merge** OR fold into #25 | One commit on `main`. |
| **4** | [#25](https://github.com/RNAX0N/flashspartan/pull/25) Smarter hashing | **Merge** after #24 *or* instead of #24 | Branch **includes #24’s commit** plus hashing work. If #24 is merged first, rebase #25 so it only adds the hashing commit. If you want one shot: merge **#25 only** and **close #24** as superseded. |
| — | Install & smoke-test | **You** | `cd packaging && ./build-package.sh -si`, plug USB, run through verify/history/hash flows. |

---

## Hold / rebase later

| PR | Status | Recommendation |
|----|--------|----------------|
| [#18](https://github.com/RNAX0N/flashspartan/pull/18) Harden device and manifest verification | **Conflicts** with `main` | **Close** or rebase after #23–#25. Much of “trust boundary” work may overlap with Paranoid / settings work on newer branches. |
| [#19](https://github.com/RNAX0N/flashspartan/pull/19) Harden verification trust boundaries | **Conflicts** with `main` | Same as #18 — pick one security-hardening PR, rebase onto current `main`, or close both and re-open a single focused PR. |

---

## Experimental / different product direction

| PR | Base | Recommendation |
|----|------|----------------|
| [#20](https://github.com/RNAX0N/flashspartan/pull/20) BadUSB behavior monitoring | `main` | **Keep draft** until Arch core path is stable. Large new subsystem (~2k LOC). Merge only if BadUSB is an explicit roadmap item. |
| [#21](https://github.com/RNAX0N/flashspartan/pull/21) Windows compatibility layer | **`cursor/badusb-monitor-4fd4` (#20)** | **Do not merge before #20.** Stacks on BadUSB branch, not `main`. Treat as a separate track (Windows port), not part of Arch install review. |

---

## Stacking note (#24 vs #25)

```
main
 └── 07aa0c7  (#24 verify history / wizard / Paranoid)
      └── a32265d  (#25 smarter hashing only)
```

- **Merge #24 then #25:** Rebase `cursor/smarter-hashing-546f` onto `main` after #24 lands; PR #25 should show ~1 commit.
- **Merge #25 only:** Closes both feature sets in one PR; **close #24** with comment “Superseded by #25.”

---

## After merge checklist

1. `git pull` on `main`
2. `cd packaging && ./build-package.sh -si`
3. `flashspartan --version` → expect **1.4.2** (plus pkgver suffix for git builds)
4. Mark drafts **Ready for review** only for PRs you’re actively testing
5. Consider **closing** stale conflict PRs (#18/#19) with a short “superseded by …” to get back to ≤3 open feature PRs

---

*Generated for install-first rollout; adjust if priorities shift (e.g. BadUSB/Windows track).*
